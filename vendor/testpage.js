$(document).ready(function () {
        getCountFrom('index.php', 0);
        getCountFrom('indexFiliere.php', 1);
        getCountFrom('indexclasse.php', 2);
        getCountFrom('Find.php', 3);
    });